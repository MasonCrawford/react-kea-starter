import React from "react";
import { kea, useValues, useActions } from "kea";

const logic = kea({
  // add your logic here
});

export default function App() {
  // const {} = useValues(logic);
  // const {} = useActions(logic);

  return (
    <div>
      <img src="https://kea.js.org/img/logo.svg" alt="Kea Logo" />
    </div>
  );
}
